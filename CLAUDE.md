# gccity — Claude 가이드

## 정체

카카오톡 **오픈채팅방을 따라가며 대화를 모으는 단일 사용자용 콘솔.**
지금 목표는 방 하나지만 구조는 여러 방을 견딘다 — 방마다 "팔로우" 를 켜고 끄는 방식이라
숫자를 늘려도 코드가 바뀌지 않는다.

`speciai-kakao-bot`(거래처 단톡방 콘솔)에서 알림 파싱·멱등·심장박동의 **교훈만** 가져왔고,
방을 고르는 방식과 발신은 정반대다.

| | speciai-kakao-bot | gccity |
|---|---|---|
| 방 고르기 | 방 안에서 `#등록 <거래처명>` | **대시보드에서만.** 방 안에 아무것도 안 친다 |
| 매칭 축 | 방 **이름** 문자열 규칙 | **`chat.channelId`**(API2). 이름은 표시용 힌트일 뿐 |
| 카톡에 쓰기 | 있다(발신 큐) | **없다.** 읽기 전용 |
| 거르는 곳 | 서버 (방이 많아서) | **단말** (방이 하나뿐이라 비용이 0) |
| 거래처·워크스페이스 | 있다 | 없다. 단일 테넌트 |

**범위 밖**: 발신·자동응답, 거래처·워크스페이스·멤버십, 방 안 명령어, AI 요약·분류.

---

## 절대 원칙

### 1. 이 봇은 카톡에 한 글자도 쓰지 않는다

`bot/gccity-bot.js` 에 `Api.replyRoom`·`bot.send`·RemoteInput·`chat.reply` 가 **없다.**
넣지 말 것. 이유가 셋이다:

- 오픈채팅방 사람들에게 그대로 보인다. 조용히 따라가는 것이 목적인데 존재를 알린다.
- 기계적 응답 패턴은 카카오의 자동화 탐지를 부른다.
- **계정이 정지되면 수집까지 함께 멈춘다.**

같은 이유로 방 안 명령어(`#등록` 같은 것)도 만들지 않는다. 방 선택은 전부 대시보드다.
알림을 읽기만 하므로 카톡의 '읽음' 표시도 생기지 않는다.

### 2. 거르는 곳은 단말이다

팔로우하지 않은 방의 본문은 **폰을 벗어나지 않는다.** `onChat` 이 `channelId` 를 읽은 직후
팔로우 여부를 보고, 아니면 본문을 만들지도 첨부를 열지도 않고 빠져나온다.

`speciai-kakao-bot` 은 방이 많아 서버에서 걸렀다(`DEVICE_FILTER=false`). 여기는 방이
하나뿐이라 단말에서 거르는 비용이 0 이고, 그러면 개인 대화가 전송 구간을 아예 지나가지
않는다. **그 설정을 복사해 오지 말 것.**

서버도 같은 순서를 지킨다 — `ingestBatch` 가 **팔로우 확인을 본문 저장보다 먼저** 한다.
이 순서를 뒤집는 순간 개인 대화가 DB 에 남는다.

방 찾기 중 후보 방의 본문은 `_buf` 에 담기지만 그것도 **폰 안이다.** 서버로 가는 것은
여전히 앞 12자뿐이고, 사람이 그 방을 팔로우해야 비로소 올라간다. 이 버퍼를 전송 큐
(`_qSeen`)에 섞지 말 것 — 그러면 개인 카톡 본문이 그대로 서버로 간다.

사진도 같은 순서다. 알림 훅은 첨부의 **주소만** 적어두고(`_attBySender`), 바이트를 읽는 것은
그 메시지가 팔로우 방 것이라고 확인된 뒤다(`finalize` → `readImage`). 개인 카톡 사진은
폰 메모리에도 안 올라간다. 서버도 같다 — `storePhoto` 가 팔로우 확인을 base64 디코딩보다
먼저 한다. 이 순서를 뒤집지 말 것.

### 3. fail-closed

설정(`/api/bot/config`)을 한 번도 못 받았으면 봇은 아무것도 보내지 않는다.
"무엇을 거를지 모르는 채로 거른다" 는 뜻이 되기 때문이다.
전부 보내는 쪽으로 실패시키지 말 것.

### 4. 방 찾기 모드는 반드시 시한부다 (그리고 이제 기본 경로가 아니다)

방을 고르는 기본 경로는 **대시보드에 channelId 를 치는 것**이다(`AddRoom` → `addRoomByChannelId`).
그 길은 개인 카톡을 한 건도 흘리지 않는다. 방 찾기 모드는 숫자를 모를 때의 보조 수단이다.


켜져 있는 동안 **이 폰에 오는 모든 카톡방**(개인 카톡 포함)의 발신자명과 본문 앞 12자가
서버로 올라온다. 사람이 끄는 걸 잊으면 그게 계속 쌓인다.
그래서 서버가 시각을 못 박고(`GCCITY_DISCOVERY_MINUTES`, 기본 30분, 상한 180분) 지나면
스스로 꺼지며, 꺼질 때 미리보기를 지운다(`expireStalePreviews`).
**무기한 옵션을 만들지 말 것.**

봇의 본문 버퍼(`_buf`)도 이 시한을 함께 따른다 — 방 찾기가 꺼졌다는 설정을 받는 즉시
버린다. 상한도 둔다(방 40개 × 방당 80건). 개인 카톡이 많은 폰에서 무한정 늘지 않게.

### 5. 조용히 성공하는 실패를 만들지 말 것

`speciai-kakao-bot` 이 가장 비싸게 배운 것이다 — 봇은 `POST 200` 을 받고, 서버는 열심히
일하고, 화면은 멀쩡한데 데이터가 어디로도 가지 않는 상태. 며칠 뒤 사람이 "요즘 안 들어오네"
할 때까지 아무도 모른다.

그래서 이 앱은:
- 라우트가 실패하면 **사유 문자열을 그대로** 응답에 싣는다(봇 로그에 찍힌다)
- 봇이 실패 코드와 응답 앞부분을 로그에 남긴다
- 대시보드가 봇 심장박동을 **폴링으로** 갱신한다
- 팔로우 방이 6시간 조용하면 화면이 경고한다(폰을 볼 때가 됐다는 뜻이다)
- 봇이 심장박동에 **빌드 표식·API2 여부·수신 건수**를 실어 보내고 화면이 그대로 보여준다.
  API2 가 꺼져 있으면 channelId 가 아예 없어 수집이 0 건인데, 그 상태는 "방이 조용함" 과
  겉보기가 같다. `⚠️ API2 꺼짐` 배지가 그 둘을 가른다

### 6. 단일 테넌트. 별도 Supabase 프로젝트

워크스페이스·RLS 정책·멤버십을 도입하지 말 것.
`speciai-kakao-bot` 은 linktalk 과 DB 를 공유해 `is_workspace_member()` 가 두 테이블의
합집합을 봐야 하는 상태다. 세 번째 앱을 그 DB 에 얹지 말 것.

테이블은 RLS 를 **켜고 정책을 하나도 만들지 않는다.** 브라우저는 Supabase 에 직접 말하지
않고 서버 라우트만 service-role 로 접근한다. `NEXT_PUBLIC_SUPABASE_ANON_KEY` 를 추가하지
말 것 — RLS 가 꺼진 테이블은 anon 키로 그대로 읽힌다.

---

## 방을 무엇으로 못 박는가 — `channelId` 다

**2026-08-20 전환.** 그전에는 알림 신원(`sbn.getTag()`/`getId()`)을 열쇠로 썼다.
지금은 메신저봇R **API2** 의 `chat.channelId` 하나만 본다.

```
chat.channelId = 18409238712050393   ← 카톡이 방에 붙인 고유 번호. 이게 정답이다
chat.room      = "신동규"             ← 방 제목이 아니다(알림 제목). 매칭에 쓰지 말 것
알림 extras 의 방 제목 칸            → 전부 빈 값. 이름 경로는 이 폰에서 세 번 다 막혔다
```
(실측: `speciai-network` 브리핑 봇 폰, 2026-08-20)

**왜 바꿨나.** 알림 열쇠는 카톡 재설치·업데이트로 바뀐다. 바뀌면 같은 방이 **새 방**으로
보이고, 팔로우 목록에 없으니 봇은 아무것도 안 보낸다 — 서버도 화면도 정상인 채로 수집만
멈춘다. `channelId` 는 방 자체의 번호라 흔들리지 않고, **사람이 눈으로 읽어 대시보드에
그대로 칠 수 있다.** 열쇠가 바뀌었는지 추적할 일 자체가 없어졌다.

이름은 여전히 매칭에 쓰지 않는다. `chat.room` 이 실려 오면 `name_hint` 로 화면 표시에만
쓰고, 사람이 붙인 `display_name` 이 언제나 우선이다. **매칭에 되돌리지 말 것.**

`channelId` 는 **끝까지 문자열로** 다룬다. 17~19자리라 JS `number` 로 만지는 순간 끝자리가
뭉개진다(`normalizeChannelId` 테스트 참조). 사람이 로그에서 `ch=[…]` 를 통째로 붙여넣어도
숫자만 뽑아 쓴다 — 안 맞는 값이 조용히 등록되는 것이 이 앱에서 제일 나쁜 실패다.

### ⚠️ 이 설계의 새 리스크: API2 가 꺼지면 전부 멈춘다

`channelId` 는 **API2 에서만** 온다(앱 공지: "레거시 API 에서 channelId·userHash·logId 를
이용하지 못합니다 (API2 제외)"). 메신저봇R 에서 프로젝트를 API2 로 만들지 않았거나 앱을
갈아엎으면 `BotManager`·`Event` 가 없고, 봇은 방을 가를 수단이 없다.

그때 봇은 **멈추지 않고 심장박동만 계속 보낸다** — `api2=0` 을 실어서. 대시보드 봇 상태 줄에
`⚠️ API2 꺼짐` 이 뜬다. 여기서 "그냥 throw 하고 죽자" 로 바꾸지 말 것. 죽으면 화면에는
"봇 끊김" 만 남아 원인을 폰에 가서 찾아야 한다.

`bot/observe.js` 를 폰에 붙이면 `[CH]` 줄에 이 폰으로 오는 방들의 channelId 가 찍힌다.
그 숫자를 대시보드에 그대로 넣으면 등록이 끝난다.

---

## 방을 고르는 흐름

**기본 — channelId 를 안다면 이게 전부다.**

```
① 폰의 다른 봇 로그(또는 bot/observe.js 의 [CH] 줄)에서 그 방의 channelId 를 읽는다
② 대시보드 [방 고르기] 에 숫자와 이름을 넣고 [방 등록]      → followed=true, config_version++
③ 봇이 설정을 다시 받아간다 (최대 60초)                      → 그때부터 대화가 쌓인다
```

이 길은 개인 카톡을 **한 건도** 서버로 올리지 않는다. 이름은 비워도 되고 나중에 [이름]
버튼으로 언제든 바꾼다 — channelId 를 방 이름으로 쓰면 화면이 숫자 무더기가 된다.

**숫자를 모를 때 — 방 찾기 모드(보조).**

```
① 대시보드에서 "방 찾기 모드" 켜기        → 서버가 discovery_until 을 30분 뒤로 찍는다
② 봇이 설정을 받아간다 (최대 60초)        → discovery=true
③ 목표 방에서 메시지가 온다               → 봇이 (channelId, 발신자, 앞 12자) 만 올린다
④ 후보 목록에 뜬다                        → 사람이 발신자·미리보기로 그 방을 알아본다
⑤ [이 방 팔로우] 클릭                     → rooms.followed=true, config_version++
⑥ 봇이 설정을 다시 받아간다               → 그 시점부터 본문이 저장된다
⑦ 방 찾기 모드 끄기                       → 다른 방 미리보기가 지워진다
```

**⑥ 이전의 대화도 함께 올라간다 — 단, 방 찾기를 켠 뒤의 것만.**
봇이 방 찾기 중 후보 방의 본문을 **폰 메모리에만** 담아두다가(`bufferForFollow`),
팔로우되는 순간 그 방 몫만 올린다(`releaseBuffer`). ①~⑤ 사이에 오간 대화가
날아가지 않는다 — 오픈채팅은 그 몇 분에도 수십 건이 지나간다.

**그 이전의 대화는 되살릴 수 없다.** 메시지 이벤트는 지나가면 끝이다.
방 찾기가 꺼지면 팔로우되지 않은 방의 버퍼는 즉시 버려진다(`clearBuffer`).

`display_name`·`name_hint` 가 둘 다 비면 화면은 `#…` + channelId 뒤 8자를 보여준다.

---

## 오픈채팅이라 다른 것

거래처 단톡방과 트래픽 성격이 다르다. **여기가 두 프로젝트의 실질적인 코드 차이다.**

### ① 수집 입구는 메시지 이벤트 하나다

`Event.MESSAGE` → `onChat` 이 전부다. 알림에서 대화를 긁던 코드(`extractMessages`)는
없앴다 — 알림은 channelId 를 주지 않아 어느 방 것인지 알 수 없기 때문이다.

알림 훅은 남아 있지만 역할이 하나다: **첨부의 주소를 적어두는 것**(`onKakaoNoti`).
여기서 대화를 다시 수집하지 말 것. 같은 메시지가 두 번 들어와 중복이 된다.

그 대신 잃은 것이 있다. 알림의 `android.messages` 배열에는 최근 8~9건이 함께 실려 와서
봇이 잠깐 죽었다 살아날 때의 복구 경로였는데, 메시지 이벤트에는 그게 없다.
**봇이 죽어 있던 동안의 대화는 되살릴 수 없다.** 그래서 심장박동 감시가 전보다 중요해졌다.

### ② 첨부는 두 이벤트를 짝지어 얻는다

같은 카톡 메시지가 두 경로로 들어온다 — 메시지 이벤트(channelId 를 준다)와
알림(첨부 `uri`·`type` 을 준다). 순서는 폰 사정에 따라 뒤바뀐다.

그래서 첨부가 딸렸을 법한 메시지(`looksLikeAttachment`)는 `_pend` 에 넣고 최대
`ATTACH_WAIT_MS`(2.5초) 기다렸다가 마저 처리한다. 알림이 그사이 오면 곧바로 붙는다.
짝짓기 열쇠는 **발신자명**이다(알림에는 channelId 가 없다). 같은 이름이 서로 다른 방에서
동시에 사진을 보내면 어긋날 수 있다 — 아는 한계다.

첨부는 **한 번만 쓴다**(`takeAtt` 이 꺼내면 지운다). 안 지우면 뒤따르는 텍스트 메시지에
같은 사진이 또 붙는다. speciai-kakao-bot 이 실측으로 물린 버그다(2026-08-14).

알림 URI 의 읽기 권한은 **알림이 살아 있는 동안만** 유효하다. 그래서 대기 시간도 첨부 주소의
수명(`ATT_TTL_MS`, 20초)도 짧다. 길게 늘리지 말 것 — 늘려봐야 읽기가 실패한다.

### ③ 멱등키 1순위는 `logId` 다

`msg_id = 'log:<chat.logId>'` (`msgIdFor`). API2 가 메시지마다 주는 고유 번호라 같은
메시지를 몇 번 다시 받아도 한 건이다.

logId 를 못 얻으면 `'msg:<수신 ms>:<md5(발신자|본문)>'`, 시각조차 없으면
`'fb:<수신 초>:<md5>'` 로 떨어진다. **마지막 폴백은 같은 초의 동일 메시지를 합쳐버린다.**
그래서 인입 라우트가 `⚠️시각없음=N` 을 로그에 남긴다.

시각은 **폰이 메시지를 받은 시각**이다(메시지 이벤트에는 카톡의 원래 시각이 없다).
봇이 큐에 넣을 때 못 박아 보내므로, 재전송으로 늦게 도착해도 값이 흔들리지 않는다.

`(room_id, msg_id)` 는 부분 인덱스가 아니라 **완전 유니크**라 supabase-js 의
`upsert(onConflict, ignoreDuplicates:true)` 가 정상 동작한다.

### ④ 배치로 보낸다 — 사진만 빼고

메시지 1건 = HTTP POST 1건이면 폰 배터리와 서버리스 호출 요금을 그대로 태운다.
`BATCH_MS`(1.5초) 또는 `BATCH_MAX`(30건) 중 먼저 닿는 쪽에서 묶어 보낸다.

**사진은 한 장에 한 요청이다**(`/api/bot/photo`). base64 는 원본의 4/3 이라 30건짜리
배치에 사진 두 장만 섞여도 Vercel 함수 본문 상한(4.5MB)을 넘고, 그러면 **그 배치의 텍스트까지
전부** 413 으로 튕긴다. 사진 한 장 때문에 대화가 통째로 사라지는 셈이다.

사진 대기열은 얕다(`PHOTO_QUEUE_MAX` 6장, 재시도 5회). 넘치거나 끝내 실패하면 그 자리에
**"사진 — 받지 못했다"** 흔적을 남긴다(`noteLostPhoto`). 조용히 사라지게 두지 말 것.

### ⑤ 발화자는 오픈채팅 닉네임이다

본명이 아니고 언제든 바뀌며 동명이인이 있다. `side='us'/'partner'` 같은 판정을 넣지 말 것.
닉네임 문자열을 그대로 저장하고 동일인 추적은 하지 않는다.

### ⑥ 오픈채팅에서 `chat.author.name` 은 발화자가 아니다 (2026-08-21 실측)

두 오픈채팅방에서 들어온 17건의 발화자가 **전부 `오픈채팅봇`** 이었다. 서로 다른 사람의 말인데
같은 이름이 붙었고, 같은 값이 `chat.room`(=`name_hint`)에도 들어 있었다. 이름 칸이 비어
알림 제목으로 떨어졌다는 뜻이다. 화면에서는 모든 말풍선이 한 사람 것으로 묶여
**"다른 사람 말이 안 들어온다"** 로 보인다 — 실제로는 들어오는데 이름만 뭉개진 것이다.

진짜 닉네임은 **알림의 `android.messages[].sender_person`** 에만 있다. 그래서 봇이:

- 알림 훅에서 그 배열을 **끝까지 훑어** (본문 앞 60자 → 닉네임) 색인을 만든다(`indexSenders`)
- 메시지 이벤트의 이름이 껍데기면(`senderLooksBogus`: 별칭이거나 단톡방인데 방 이름과 같음)
  그 색인에서 찾아 바꾼다(`resolveSender`)
- 알림이 늦게 올 수 있어 최대 `SENDER_WAIT_MS`(1.2초) 붙들었다가 보낸다

**첨부(`notiAttachmentInfo`)와 정반대의 판단이다** — 첨부는 배열의 마지막 것만 봐야 하고
(옛 사진이 새 메시지에 다시 붙는다) 이름 색인은 많을수록 좋다. 섞지 말 것.

**★ 2026-08-21 두 번째 실측 — `chat.author.name` 은 굳는다.** 17시 25분 이후 18건이 전부
`축하하는 죠르디` 로 저장됐다(실제로는 여섯 명). 예전에는 `오픈채팅봇` 으로 뭉개져
`SENDER_ALIASES` 가 걸렀는데, 이번엔 **그럴듯한 진짜 닉네임**으로 굳어 그 그물을 통과했다.
그래서 `resolveSender` 를 뒤집었다 — **알림 색인이 먼저고 `author.name` 은 그 다음이다.**
색인은 (본문 앞 60자 + 카톡이 찍은 시각)으로 맞추므로 굳지 않는다.
되돌리지 말 것. 형제 프로젝트(`speciai-kakao-bot`)는 같은 이유로 API2 수집을 통째로 껐다.

봇이 심장박동에 `sidx`(색인이 준 이름) · `sauth`(author 로 때운 이름) 를 실어 보내고,
화면 봇 상태 줄에 `이름 알림 12/API2 3` 으로 뜬다. **`sauth` 만 늘고 있으면 알림이 이름을
주지 않는다는 뜻**이라 폰의 알림 권한·미리보기 설정을 봐야 한다.

짝짓기 열쇠는 본문이지만 **본문만으로 가르지 않는다.** 오픈채팅에서 `ㅋㅋ`·`네`·`감사합니다` 는
하루에도 수십 번이라, 겹칠 때마다 이름을 포기하면 화면이 다시 `오픈채팅봇` 으로 덮인다.
그래서 같은 문구에 후보를 여럿 매달아 두고(`putSender`) **알림의 `time` 으로 고른다**
(`lookupSender`) — 카톡이 찍은 메시지 시각이라 봇이 메시지 이벤트를 받은 시각과 1~2초 안쪽이다.

두 후보가 `SENDER_TIE_MS`(4초) 안에서 갈리지 않을 때만 손대지 않는다. 틀린 이름을 붙이는
것이 뭉뚱그린 이름보다 나쁘다.

**색인은 폰 메모리에만 있고 20초면 사라진다.** 서버로 가는 것은 팔로우 방으로 확정된
메시지에 붙은 닉네임뿐이다. 이 색인을 전송 큐에 섞지 말 것 — 개인 카톡 본문 앞 60자가
그대로 서버로 간다.

알림이 이름을 하나도 안 주면 봇이 1분에 한 번 `알림에 발화자가 없다 (… title="…")` 를
찍는다. 그 줄이 계속 보이면 이 폰에서는 이름 보정 자체가 불가능하다는 뜻이다 —
`bot/observe.js` 의 `[MSGS]` 줄로 확인할 것.

---

## 파일 지도

```
bot/gccity-bot.js          수집 봇. 읽기 전용. API2 전용. Rhino ES5
bot/observe.js             진단 스크립트. 서버 없이 돈다 — [CH] 줄에 방들의 channelId 를 찍는다
src/middleware.ts          로그인 게이트. ★ 봇 경로는 PUBLIC_PATHS 로 반드시 제외
src/server/state.ts        app_state — 방 찾기 모드 · config_version · 심장박동 · 봇 상태
src/server/ingest.ts       인입 본체. channelId 정규화·멱등키·미리보기. 순수 함수는 테스트가 있다
src/server/photos.ts       사진 저장(room-photos 버킷)과 서명 URL
src/server/rooms.ts        방 목록·등록(channelId)·팔로우·이름·삭제, 메시지 조회
src/server/files.ts        자료실(room-files 버킷). 사람이 손으로 올리는 문서
src/server/complaints.ts   민원실. 담기·상태·붙여넣기 파서. 순수 함수는 테스트가 있다
src/server/complaint-crawl.ts  민원 출처 크롤. ★ robots.txt 를 먼저 본다
src/server/complaint-classify.ts  민원/처리/공지 가르기와 처리 소요일. 전부 순수 함수 + 테스트
src/server/digest.ts       카톡 대화 → 민원 초안(Claude API). ★ 초안이지 확정이 아니다
src/app/api/bot/config     봇이 받아가는 설정 (팔로우 channelId 목록 + 방 찾기 모드)
src/app/api/bot/ingest     봇 인입 (배치 · 텍스트와 파일 이름)
src/app/api/bot/photo      봇 인입 (사진 한 장 = 요청 한 번)
src/app/api/state          대시보드 폴링 — 화면 상태 전부
src/app/api/rooms          대시보드 조작 (channelId 등록·팔로우·이름·삭제·방 찾기 토글)
src/app/api/files          자료실 (서명 업로드·목록·내려받기)
src/app/api/complaints     민원실 (목록·담기·상태·출처·지금 긁기)
src/app/api/cron/complaints  민원 출처 자동 수집. ★ CRON_SECRET 을 스스로 검사한다
src/app/api/cron/digest      카톡 분석 자동 실행(6시간). ★ 창은 마지막 성공 실행의 끝부터
src/components/Dashboard.tsx  화면 전부. 3초 폴링
```

### ⚠️ 쿠키 없이 불리는 라우트는 `PUBLIC_PATHS` 에 반드시 추가할 것

빠뜨리면 미들웨어가 로그인 화면으로 307 리다이렉트하고, 봇은 HTML 을 받아 **조용히**
실패한다. 봇 로그에는 성공처럼 보이는 응답만 남는다.
(`speciai-kakao-bot` 이 같은 자리에 같은 경고를 달아둔 이유다.)

`/api/cron/` 도 같은 처지다(Vercel Cron 이 부른다). 다만 그쪽은 문지기가 라우트 안에만
있으므로 — `CRON_SECRET` 검사 — 그 검사를 지우면 민원 크롤이 누구에게나 열린다.

---

## 봇 ↔ 서버 프로토콜

```
GET  /api/bot/config?build=<빌드>&api2=<0|1>&msgs=<수신건수>    헤더 X-Ingest-Token
  ← { ok, version, discovery, discoveryUntil, follow: ["<channelId>", …] }

POST /api/bot/ingest      헤더 X-Ingest-Token
  → { msgs: [{channelId, nameHint, group, sender, text, tsMs, logId,
               att?: {kind:'file'|'image', name, mime}}],      // 팔로우 방만
      seen: [{channelId, nameHint, group, sender, preview, tsMs}] }  // 방 찾기 모드일 때만
  ← { ok, inserted, skipped, dropped, seen, configVersion, discovery }

POST /api/bot/photo       헤더 X-Ingest-Token          // 사진 한 장 = 요청 한 번
  → { channelId, sender, text, tsMs, logId, name, mime, b64 }
  ← { ok, stored, reason, bytes }
```

**봇에게 내려보내는 것은 channelId 뿐이다.** 이름을 내려보내지 말 것 — 봇은 이름으로 방을
가리지 않고, 화면 이름은 사람이 붙인 것이라 봇의 판정과 아무 관계가 없다.
(`speciai-kakao-bot` 은 교정된 표시 이름을 봇에 내려보냈다가 발신이 전부 단말에서
거부됐다. 같은 종류의 착오다.)

`att.kind='file'` 은 **이름과 형식만** 온다. 카톡 파일의 바이트는 가져오지 않는다(결정,
2026-08-20) — 실물은 사람이 자료실에 끌어다 넣고, 대화에는 "무엇이 왔는지" 만 남는다.
`att.kind='image'` 가 배치에 실려 오는 경우는 봇이 사진을 **끝내 못 올린** 때뿐이다.

`configVersion` 이 응답에 실려 온다. 봇이 가진 값과 다르면 즉시 설정을 다시 받아간다 —
방이 활발하면 팔로우 변경이 몇 초 안에 반영되고, 조용하면 최대 `CONFIG_POLL_MS`(60초).

---

## 봇이 살아 있는가

봇은 60초마다 설정을 받아가고, 그 왕복이 곧 심장박동이다(`touchHeartbeat`).
`api/bot/config` 와 `api/bot/ingest` 둘 다 찍는다.

| 화면 | 조건 | 뜻 |
|---|---|---|
| ● 정상 | 3분 이내 | 앱이 돌고 있다 |
| ● 지연 | 3~30분 | 절전(Doze). 알림이 오면 폰이 깨어나므로 수집은 대개 계속된다 |
| ● 끊김 | 30분 초과 | 앱이 죽었거나 폰이 재부팅됐다. **사람이 폰을 봐야 한다** |

**`CONFIG_POLL_MS` 를 3분 위로 올리지 말 것** — 멀쩡한 봇이 늘 "지연" 으로 뜬다.
거짓 경보는 경보가 없느니만 못하다. 한 번 헛울면 다음부터 아무도 안 본다.
반대로 줄이면 호출 수가 그대로 요금이 된다(`speciai-kakao-bot` 은 15초 폴링으로 월 17만 번을
두드려 Vercel 402 로 멈춘 적이 있다).

**직전 간격(`bot_last_gap_ms`)을 같이 남기는 이유**: "재워짐" 과 "죽음" 을 가르는 유일한
단서다. Doze 는 간격만 벌리고 신호는 계속 오지만, 앱이 죽으면 끊긴다. 아끼지 말 것.

**⚠️ "마지막 수집" 과 헷갈리지 말 것.** 방 카드의 그 값은 저장된 메시지의 시각이라
방이 조용하면 며칠 전이어도 봇은 멀쩡하다. 화면에서 두 줄을 분리해 둔 이유다.

### 봇이 자기 상태를 같이 보낸다

설정 요청 쿼리에 `build`(폰에 올라간 스크립트 표식) · `api2`(0/1) · `msgs`(켜진 뒤 받은
카톡 메시지 수)가 실린다. 화면 봇 상태 줄이 그대로 보여준다.

| 화면에 보이는 것 | 뜻 |
|---|---|
| `2026-08-20-ch1` | 폰에 실제로 올라간 스크립트. 붙여넣기가 먹었는지 여기서 본다 |
| `⚠️ API2 꺼짐` | channelId 를 못 얻는다. 메신저봇R 에서 API2 프로젝트로 다시 만들 것 |
| `수신 0건` 이 계속 | API2 는 켜졌는데 메시지 이벤트가 안 온다 → 알림 접근 권한·카톡 알림 설정 |

`BUILD` 를 고칠 때마다 올릴 것. 이게 없어서 "옛 코드가 도는 중" 을 "코드가 잘못됐다" 로
오진한 적이 있다(`speciai-network` 브리핑 봇).

### ⚠️ 재컴파일은 옛 루프를 죽이지 않는다

메신저봇R 은 스크립트를 다시 컴파일해도 **이전에 띄운 데몬 스레드를 그대로 둔다.**
옛 스레드는 옛 스크립트의 변수를 붙들고 있어서, 새 코드가 아무리 멀쩡해도 옛 설정으로
계속 돈다. 컴파일 열 번이면 루프 열 개가 60초마다 서버를 두드린다 — 조용히 요금만
곱해지는 쪽이라 실패보다 위험하다.

그래서 `startLoop` 이 세대 번호를 **JVM 시스템 속성**(`gccity.loop.generation`)에 찍고,
루프가 매 틱마다 자기 세대와 비교해 다르면 스스로 빠져나온다. 스크립트 변수로는 안 된다 —
옛 스레드는 옛 변수를 보기 때문에 새 스크립트가 무엇을 넣든 보이지 않는다.

**실측(2026-08-19)**: 주소를 채우기 전 버전이 남아 이런 로그가 60초마다 찍혔다.

```
gccity: 설정 수신 실패 code=0 … URISyntaxException: https://xn--<>-wn6iy0t7ncf10asru/
```

`xn--<>-wn6iy0t7ncf10asru` 는 **`<배포도메인>`** 이 IDN punycode 로 변환된 것이다.
플레이스홀더가 그대로 남은 옛 스크립트라는 뜻이지 새 코드의 문제가 아니다.
**같은 로그에 성공 줄(`설정 v… 팔로우=N개`)이 함께 보이면 그 진단이 확정이다** —
새 코드는 멀쩡하고 좀비만 짖고 있는 것이다. 서버의 `bot_last_seen_at` 으로 교차 확인할 것.

---

## 스키마

| 테이블 | 역할 |
|---|---|
| `rooms` | 방. `channel_id` 유니크. `followed` 가 수집 여부의 전부 |
| `messages` | 메시지. `(room_id, msg_id)` 유니크가 멱등의 전부. `attachment_*` 는 사진·파일 |
| `files` | 자료실. 사람이 손으로 올리는 문서 (`room-files` 버킷) |
| `app_state` | 단일 행. 방 찾기 모드 · `config_version` · 심장박동 · 봇 상태 |
| `complaints` | 민원. `dedup_key` 유니크가 멱등의 전부. **방에 매이지 않는다** |
| `complaint_sources` | 민원 크롤 출처. 마지막 실행 결과·실패 사유가 여기 남는다 |
| `complaint_authors` | 작성자 명부(기관·주민·숨김). 민원과 공지를 가르는 첫 축 |
| `digest_runs` | 카톡 분석 실행 기록. **다음 창의 시작점**이 여기서 나온다. 실패 사유도 |

버킷 둘의 성격이 다르다 — `room-photos` 는 **대화에 딸린 사진**(봇이 올린다),
`room-files` 는 **사람이 챙겨 넣는 문서**다. 섞지 말 것. 섞으면 계약서를 찾으러 들어간
자료실이 방금 지나간 짤로 뒤덮인다.

`messages.attachment_path` 는 URL 이 아니라 **Storage 경로**다. 버킷이 비공개라 볼 때마다
서명해서 URL 을 만든다(`signPhotoUrls`, 1시간). URL 을 저장하면 만료된 주소가 DB 에 남아
"왜 사진이 안 뜨지" 로 돌아온다.

**방 행은 방 찾기 모드일 때, 또는 사람이 대시보드에서 channelId 를 칠 때만 만들어진다.**
평소에는 이 폰에 개인 카톡이 아무리 와도 서버에 흔적이 남지 않는다.

옛 알림 열쇠로 만들어진 행은 지우지 않고 남겨뒀다(대화까지 cascade 로 날아간다).
화면이 `옛 열쇠` 배지로 구분해 보여주고, 필요 없으면 사람이 지운다(`isLegacyKey`).

---

## 폰 설정이 절반이다

봇 코드가 완벽해도 이것 중 하나가 빠지면 아무것도 안 들어온다.

- 목표 오픈채팅방 **알림 ON** (방별 설정. 오픈채팅은 꺼두는 사람이 많다)
- 카톡 알림 **내용 미리보기 ON** (잠금화면 포함). 꺼져 있으면 본문이 "새 메시지" 로 온다
- 설정 → 배터리 → **미사용 앱 절전 해제** (삼성. `speciai-kakao-bot` 이 여기서 하루를 태웠다)
- 메신저봇R 배터리 최적화 제외 · 알림 접근 권한
- 봇 계정이 그 오픈채팅방에 **참여 중**일 것. 강퇴당하면 그 순간 끝난다

---

## 민원실 (2026-08-21)

과천 민원 게시글을 모아 **새 민원 → 확인 중 → 처리 완료** 로 상태를 따라가는 곳.
자료실과 나란히 서지만 성격이 반대다.

| | 자료실 | 민원실 |
|---|---|---|
| 소속 | **방마다** 갈린다 (`files.room_id` 필수) | **전역.** 방과 무관하다 |
| 들어오는 것 | 사람이 올린 파일 바이트 | 게시글의 제목·주소·상태 |
| 왜 | 계약서는 그 방의 것이다 | 민원은 도시의 일이라 방을 바꿔도 같아야 한다 |

### 1. robots.txt 를 먼저 본다. 막혔으면 긁지 않는다

`complaint-crawl.ts` 가 출처를 **등록할 때와 긁을 때 모두** 대상 호스트의 robots.txt 를 보고,
막혀 있으면 사유를 남기고 멈춘다. 상대 서버가 하지 말라고 적어둔 일이다 — 이 검사를 빼거나
"무시" 옵션을 만들지 말 것.

**실측 2026-08-21**: `cafe.naver.com` 은 모든 봇에 `Disallow: /`,
`www.gccity.go.kr` 은 `/portal/bbs/`·`/jumin/bbs/`·`/dept/bbs/` 를 막았다(게시판이 전부 거기다).
즉 이 도시의 주요 목록 둘은 **크롤 경로가 없다.** 그래서 붙여넣기가 곁다리가 아니라 주력이다.

### 2. 붙여넣기 파서가 제목만 남기는 이유

목록을 복사하면 제목 뒤에 댓글 수 `[3]`·새 글 배지 `N`·아이콘이 딸려 온다.
주소가 없는 글은 **제목이 멱등키**라, 그 부스러기를 안 떼면 댓글이 하나 늘 때마다
같은 민원이 새 건으로 쌓인다. `cleanTitle` 이 그것만 한다 — 제목 안의 대괄호는 건드리지 않는다.

### 3. 다시 긁어도 사람이 바꾼 것을 덮지 않는다

`addDrafts` 는 `upsert(..., ignoreDuplicates: true)` 다. 있는 글은 **손대지 않는다.**
"처리 완료" 로 옮겨둔 민원이 다음 크롤에 "새 민원" 으로 되돌아가면 이 화면을 아무도 안 믿는다.
`onConflict` 를 갱신 쪽으로 바꾸지 말 것.

### 4. 실패를 화면에 남긴다

출처 줄에 마지막 실행 결과가 그대로 뜬다 — `12건 중 3건 새로` 또는 `⚠️ 실패 — 사유`.
링크 패턴이 안 맞아 **0건** 인 것과 게시판이 조용한 것은 겉보기가 같아서, 크롤러는 0건을
성공이 아니라 **실패로 취급하고 사유를 적는다**(`목록에서 글 링크를 하나도 찾지 못했다`).

### 5. 자동 수집은 fail-closed

`/api/cron/complaints` 는 `CRON_SECRET` 이 없으면 503 으로 아무것도 하지 않는다.
쿠키가 없는 경로라 그 토큰이 문지기의 전부다. 하루 한 번(`vercel.json`)이고, 사람이
[지금 긁기] 로 언제든 앞당긴다. 폴링 간격을 짧게 만들지 말 것 — 남의 서버를 두드리는 일이다.

### 6. 목적은 목록이 아니라 흐름이다 (2026-08-21)

민원이 **언제 들어와, 며칠 만에, 어떻게** 처리됐는가를 재는 것이 이 화면의 목적이다.
그래서 같은 목록 안에 성격이 다른 글이 섞여 있다는 것을 스키마가 인정한다 —
`complaints.kind` 가 `report`(주민 민원) · `resolution`(처리·회신) · `notice`(공지·홍보) ·
`unknown`(모르는 작성자) 넷으로 갈린다.

**★ 제목의 날짜는 접수일, 작성일은 회신일이다.** 정책관 계정의 처리 글에서 실측했다.

```
"2026년 8월 7일(금) 과천대로 관문체육공원 도로 민원"  작성일 08.11 → 4일
"2026년 8월 12일(수) 펜타원 통근 버스 … 민원"         작성일 08.13 → 1일
"2026년 8월 20일(목) 양재천 냄새 민원"                작성일 08.20 → 당일
```

즉 **짝짓기 없이도 처리 글 하나로 소요일이 나온다.** 짝짓기(`resolution_of`)는 그 위에
얹는 정밀도지 전제가 아니다. 그리고 **자동으로 엮지 않는다** — 제목이 비슷하다고 이으면
통계가 조용히 틀린다. 사람이 화면에서 [민원에 잇기] 로 잇는다.

가르는 순서는 셋뿐이고 그 순서가 전부다(`classify`):
1. 제목이 `날짜 + …민원` 꼴 → **처리 기록**. 작성자가 누구든 상관없다
2. 작성자가 기관 → 공지
3. 아니면 주민 민원. 작성자를 모르면 `unknown` 으로 남기고 사람에게 묻는다

**작성자만으로 가르면 틀린다.** `조인길 정책관` 한 계정이 민원 회신과 보도자료를 함께 쓴다.
그래서 명부는 기본값만 주고 제목 규칙이 그 위에 온다. 규칙을 뒤집지 말 것.

사람이 화면에서 종류를 고치면 `kind_locked` 가 서고, 그 뒤 [지난 글 다시 가르기]가
그 행을 덮지 않는다. 명부를 손볼 때마다 고쳐둔 것이 되돌아가면 아무도 화면을 고치지 않는다.

통계에는 **모수(`measured`)를 같이 싣는다.** "평균 3일" 이 두 건에서 나온 값인지 백 건에서
나온 값인지 모르면 그 숫자는 판단 근거가 못 된다.

### 7. 카톡 분석은 초안만 만든다 (2026-08-21)

사람이 말풍선마다 [민원] 을 누르는 길은 정확하지만 놓친다. 그래서 6시간마다
(`/api/cron/digest`) 그동안 쌓인 대화를 모델에게 주고 민원을 뽑아 **초안**으로 넣는다.

- **`ai_draft = true` 인 행은 화면에 `AI 초안` 배지가 붙는다.** 사람이 카페와 대조해
  [확정] 하거나 [지우기] 한다. 모델이 넣은 것을 사람이 넣은 것처럼 보이게 하지 말 것 —
  그러는 순간 이 목록 전체를 믿을 수 없게 된다
- **멱등키가 `chat:<앵커 메시지 id>` 로 사람이 담는 것과 같은 열쇠 공간이다.**
  이미 사람이 담아 상태를 바꿔둔 민원을 모델이 덮지 않는다(`ignoreDuplicates`)
- **창은 마지막 *성공* 실행의 끝부터**다(`digest_runs`). cron 이 몇 번 걸러도 구멍이 안 난다.
  단 `MAX_LOOKBACK_H`(48시간)보다 뒤로는 안 간다 — 며칠치를 한 번에 넣으면 창이 흐려진다
- **fail-closed.** `ANTHROPIC_API_KEY` 가 없으면 아무것도 안 하고 사유를 남긴다.
  대화가 0건이면 모델을 아예 부르지 않는다(빈 요청도 돈이다)
- 모델은 `claude-opus-5`, 구조화 출력(zod)으로 받는다. `GCCITY_DIGEST_MODEL` 로 바꿀 수 있다

### ⚠️ cron 주기를 하루 1회보다 잦게 두지 말 것

`vercel.json` 의 `schedule` 을 `0 */6 * * *` 로 두면 **배포가 통째로 실패한다** —
Hobby 요금제는 cron 을 하루 1회로 제한한다(실측 2026-08-21, 실패 링크가
`vercel.com/docs/cron-jobs/usage-and-pricing` 로 리다이렉트됐다).
더 자주 돌리려면 요금제를 올린 뒤 바꿀 것. 그때까지의 공백은 화면이 알려준다
(`digestDue` → "카톡 분석이 밀렸다" 배너 + [지금 분석]).

**`vercel.json` 에 `"//"` 같은 주석 키를 넣지 말 것.** 스키마에 없는 최상위 키가 있으면
역시 배포가 실패한다(같은 날 실측). 설명은 이 문서에 적는다.

### 하지 말 것

- 모델 초안을 사람 확인 없이 확정 처리하기 (배지를 떼지 말 것)
- robots 무시 옵션·로그인해서 긁기 (카페 비공개 글은 애초에 대상이 아니다)
- 제목이 비슷하다는 이유로 민원↔처리를 자동으로 잇기 (틀린 짝은 통계를 조용히 망친다)
- 평균 처리일을 모수 없이 보여주기
- 민원실을 방별로 되돌리기 (`room_id` 는 "카톡에서 담았다" 는 출처 표시일 뿐이다)
- 대시보드 3초 폴링에 민원 목록을 얹기 (읽는 중에 목록이 흔들린다. 탭 열 때만 읽는다)
- 크롤 결과를 요약해서 "완료" 로만 알리기

---

## 아직 없는 것

- **카톡 파일의 실물.** PDF·한글 같은 문서는 **이름만** 들어온다(결정, 2026-08-20).
  알림 URI 로 바이트를 읽는 것 자체는 가능하지만(`speciai-kakao-bot` 의 `uriToBase64` 에
  실증 코드가 있다), 실물은 사람이 자료실에 끌어다 넣기로 했다.
  되살릴 거면 `/api/bot/photo` 옆에 같은 모양의 라우트를 하나 더 만들고 `room-files` 에
  넣으면 된다 — 자료실 목록에 그대로 합류한다.
- **백필.** 메우는 것은 하나뿐이다 — 방 찾기 중 담아둔 본문을 팔로우 시점에 올리는 것
  (`bufferForFollow`/`releaseBuffer`). 알림 배열로 과거분을 받던 경로는 channelId 전환으로
  사라졌다(알림은 방을 특정하지 못한다). **봇이 죽어 있던 동안의 대화는 되살릴 수 없다.**
  대화 내보내기 .txt 임포트가 그 구멍의 유일한 답이다. 필요해지면 그때 만든다.
- **재시도 큐의 영속화.** 큐는 메모리에만 있다. 앱이 죽으면 밀린 것이 사라진다.
  파일 큐가 필요하면 `speciai-kakao-bot` 의 `enqueue`/`flushQueue` 를 가져올 것.

---

## 개인정보

- 오픈채팅 참여자는 **불특정 다수이고 동의를 받을 통로가 없다.** 거래처 방과 다르다.
- 닉네임이 가명이어도 대화 내용과 결합하면 개인정보가 될 수 있다.
- 방장이면 공지에 자동 수집 사실을 한 줄 남길 것.
- 팔로우하지 않은 방의 본문은 저장하지 않는다. 방 찾기 모드의 앞 12자도 시한부다.
- 민원실에 담기는 것은 **공개 게시판의 글**이지만, 닉네임과 내용을 모아 보관하는 순간
  성격이 달라진다. 목록을 밖으로 내보내지 말 것. 카톡에서 담은 민원은 원문이 통째로 남는다.

---

## 코딩 규칙

- 파일명·폴더명 kebab-case, Next.js 라우트 컨벤션 우선
- 코드 식별자는 영문. 한국어는 UI 문자열·주석에만
- 주석은 "왜" 를 적는다. 코드가 말하는 "무엇" 을 반복하지 말 것
- `bot/*.js` 는 Rhino(ES5). 화살표 함수·`let`·`const`·템플릿 리터럴 금지
- supabase-js 는 쿼리 실패를 throw 하지 않는다 — `error` 를 반드시 보고 로깅할 것

## 검증

```bash
npm run typecheck && npm test && npm run build
node --check bot/gccity-bot.js   # 봇은 붙여넣기 전에 문법만이라도 본다
```
